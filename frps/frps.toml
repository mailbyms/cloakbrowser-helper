bindPort = 7000
auth.token = "change_me_to_strong_token"

# Dashboard
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "jane"
webServer.password = "888888"

# 日志（输出到 stdout，由 Docker 收集）
log.to = "console"
log.level = "debug"
